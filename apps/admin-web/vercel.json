{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=admin-web",
  "devCommand": "cd ../.. && pnpm turbo run dev --filter=admin-web",
  "git": {
    "deploymentEnabled": false
  },
  "crons": [
    {
      "path": "/api/onboarding/mailbox-sync",
      "schedule": "*/5 * * * *"
    }
  ]
}
