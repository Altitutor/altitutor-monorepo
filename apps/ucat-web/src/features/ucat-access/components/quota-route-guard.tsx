"use client";

/**
 * Launch pages and the server-owned begin endpoint enforce quota before the
 * parameterless exam route is entered. The route itself only resumes the
 * already-authorised active attempt.
 */
export function QuotaRouteGuard() {
  return null;
}
