/** Horizontal padding — keep in sync with `Navbar` inner shell. */
export const STUDENT_SHELL_PAD_X = 'px-4 sm:px-6 lg:px-8';

/** Vertical padding for main page content. */
export const STUDENT_SHELL_PAD_Y = 'py-8 md:py-10';

/** Default max width for student app content (Tailwind 7xl). */
export const STUDENT_CONTENT_MAX = 'max-w-7xl';
