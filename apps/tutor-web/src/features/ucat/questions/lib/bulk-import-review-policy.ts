/**
 * Confidence is a workflow policy, never a response-validation constraint.
 * Values below this threshold remain available as tutor-approvable fixes.
 */
export const BULK_IMPORT_AUTO_APPLY_CONFIDENCE = 0.8
