import { UcatReconciliationShell } from '@/features/ucat/reconciliation'

export default function UcatReconciliationLayout({ children }: { children: React.ReactNode }) {
  return <UcatReconciliationShell>{children}</UcatReconciliationShell>
}
