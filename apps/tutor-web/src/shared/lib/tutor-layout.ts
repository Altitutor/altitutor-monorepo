/** Horizontal padding — keep in sync with `Navbar` inner shell. */
export const TUTOR_SHELL_PAD_X = 'px-4 sm:px-6 lg:px-8';

/** Vertical padding for main page content. */
export const TUTOR_SHELL_PAD_Y = 'py-8 md:py-10';

/** Default max width for tutor app content (Tailwind 7xl). */
export const TUTOR_CONTENT_MAX = 'max-w-7xl';
