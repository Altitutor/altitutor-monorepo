/** Pill label above section headings — matches feature card icon tint. */
export const UCAT_SECTION_EYEBROW_CLASS =
  "inline-flex rounded-full bg-marketing-primary/10 px-3.5 py-1.5 text-xs font-semibold uppercase tracking-[0.18em] text-marketing-primary sm:text-sm sm:tracking-[0.16em]";

/** Plan tier badge on light pricing cards. */
export const UCAT_PLAN_BADGE_LIGHT_CLASS = UCAT_SECTION_EYEBROW_CLASS;

/** Plan tier badge on dark pricing cards. */
export const UCAT_PLAN_BADGE_DARK_CLASS =
  "inline-flex rounded-full bg-white/10 px-3.5 py-1.5 text-xs font-semibold uppercase tracking-[0.18em] text-marketing-cream sm:text-sm sm:tracking-[0.16em]";

/** Shared vertical rhythm for main landing sections. */
export const UCAT_SECTION_PADDING_CLASS = "px-4 py-28 sm:px-8 sm:py-36";

/** Matches navbar pill and product demo width. */
export const UCAT_CONTENT_WIDTH_CLASS = "mx-auto w-full max-w-6xl min-w-0";

/** Fixed nav pill — mirrors section horizontal padding (px-4 / sm:px-8). */
export const UCAT_NAV_WIDTH_CLASS =
  "w-[calc(100%-2rem)] sm:w-[calc(100%-4rem)] max-w-6xl";

/** Section intro under h2 — Comparison reference. */
export const UCAT_SECTION_DESCRIPTION_CLASS =
  "text-lg leading-relaxed text-marketing-charcoal/65 sm:text-xl";

/** Body copy on light cards and content blocks. */
export const UCAT_BODY_DESCRIPTION_CLASS =
  "text-base leading-relaxed text-marketing-charcoal/65";

/** Smaller supporting copy on light backgrounds (FAQ answers, disclaimers). */
export const UCAT_SUPPORTING_TEXT_CLASS =
  "text-sm leading-relaxed text-marketing-charcoal/65 sm:text-base";

/** Main section h2 on light backgrounds. */
export const UCAT_SECTION_HEADING_CLASS =
  "text-4xl font-semibold tracking-[-0.035em] text-marketing-charcoal sm:text-5xl";

/** Main section h2 on dark backgrounds. */
export const UCAT_SECTION_HEADING_DARK_CLASS =
  "text-4xl font-semibold tracking-[-0.035em] text-marketing-cream sm:text-5xl";

/** Card and block h3 on light backgrounds. */
export const UCAT_CARD_TITLE_CLASS =
  "text-2xl font-semibold tracking-[-0.03em] text-marketing-charcoal";

/** Card h3 on dark backgrounds. */
export const UCAT_CARD_TITLE_DARK_CLASS =
  "text-2xl font-semibold tracking-[-0.03em] text-marketing-cream";

/** Body copy on dark cards and charcoal sections. */
export const UCAT_DARK_BODY_DESCRIPTION_CLASS =
  "text-base leading-relaxed text-marketing-cream/72";

/** Supporting copy on dark backgrounds. */
export const UCAT_DARK_SUPPORTING_TEXT_CLASS =
  "text-sm leading-relaxed text-marketing-cream/60 sm:text-base";

/** Italic student testimonial quote. */
export const UCAT_STUDENT_QUOTE_CLASS =
  "text-base italic leading-relaxed text-marketing-charcoal/65";

/** Student score / cohort metadata under a name. */
export const UCAT_STUDENT_METADATA_CLASS =
  "text-xs uppercase tracking-[0.1em] text-marketing-charcoal/65 sm:text-sm";
