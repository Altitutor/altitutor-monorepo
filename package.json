{
  "name": "workspace",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo workspace for Altitutor apps and packages",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "db:types": "npx supabase gen types typescript --local --schema public > apps/admin-web/src/shared/lib/supabase/database/generated.ts",
    "db:types:remote": "npx supabase gen types typescript --project-id \"$SUPABASE_PROJECT_ID\" --schema public > apps/admin-web/src/shared/lib/supabase/database/generated.ts"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "npm@10.9.2"
}

